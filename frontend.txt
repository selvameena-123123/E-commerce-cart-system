<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ShopEase - Colorful E-Commerce</title>
<style>
/* ===== Reset & Base ===== */
*{margin:0;padding:0;box-sizing:border-box;font-family:'Poppins',sans-serif;}
body{background:#f0f4f8;}
a{text-decoration:none;color:inherit;}
button{cursor:pointer;}

/* ===== HEADER ===== */
header{
  background:rgb(62, 156, 214);
  color:white;
  padding:20px 30px;
  display:flex;
  justify-content:space-between;
  align-items:center;
  position:sticky;
  top:0;
  z-index:1000;
  box-shadow:0 5px 15px rgba(0,0,0,0.2);
  /* border-bottom-left-radius:15px;
  border-bottom-right-radius:15px; */
}
header h1{font-size:2rem;font-weight:bold;}
.search-login{
  display:flex;
  align-items:center;
  gap:15px;
}
input[type="text"]{
  padding:8px 15px;
  border-radius:25px;
  border:none;
  outline:none;
  width:220px;
  transition:0.3s;
}
input[type="text"]:focus{box-shadow:0 0 8px rgba(255,106,0,0.7);}
button.login-btn{
  background:white;color:#ee0979;border:none;padding:8px 18px;border-radius:25px;
  font-weight:bold;transition:0.3s;
}
button.login-btn:hover{background:#fff200;color:#ee0979;transform:scale(1.05);}
#cart-count{
  background:white;color:#ee0979;font-weight:bold;border-radius:50%;padding:3px 8px;margin-left:5px;
}

/* ===== SIDEBAR ===== */
.sidebar{
  width:220px;
  background: rgb(62, 156, 214);
  height:100vh;
  position:fixed;
  top:80px;
  left:0;
  padding-top:20px;
  box-shadow:2px 0 10px rgba(0,0,0,0.1);
  /* border-top-right-radius:20px;
  border-bottom-right-radius:20px; */
}
.sidebar button{
  display:block;
  width:90%;
  margin:10px auto;
  padding:12px;
  border:none;
  background:rgba(255,255,255,0.15);
  color:white;
  font-weight:bold;
  font-size:1rem;
  border-radius:15px;
  transition:0.3s;
}
.sidebar button:hover{
  background:white;color:#ee0979;
  transform:translateX(10px);
}

/* ===== PRODUCT GRID ===== */
.container{
  margin-left:240px;
  padding:50px;
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(240px,1fr));
  gap:25px;
  object-fit: cover;

}

/* ===== PRODUCT CARD ===== */
.card{
  background:white;
  border-radius:20px;
  overflow:hidden;
  box-shadow:0 5px 15px rgba(0,0,0,0.1);
  text-align:center;
  transition:transform 0.3s,box-shadow 0.3s;
  animation:fadeIn 1s ease;
}
.card:hover{
  transform:translateY(-10px);
  box-shadow:0 15px 25px rgba(0,0,0,0.25);
}
.card img{
  width:100%;
  height:200px;
  object-fit:cover;
  transition:transform 0.4s;
}
.card:hover img{transform:scale(1.05);}
.card h3{margin:12px 0;font-size:1rem;font-weight:bold;}
.card p{font-size:0.9rem;color:gray;margin-bottom:12px;}
.card button{
  background: linear-gradient(45deg,#ff6a00,#ee0979);
  color:white;
  border:none;
  padding:10px 20px;
  border-radius:25px;
  font-weight:bold;
  transition:0.3s;
}
.card button:hover{
  background: linear-gradient(45deg,#f7971e,#ffcb05);
  transform:scale(1.05);
}

/* ===== CART ===== */
.cart{
  position:fixed;
  right:-420px;
  top:0;
  width:380px;
  height:100vh;
  background:#fff;
  box-shadow:-5px 0 15px rgba(0,0,0,0.3);
  transition:right 0.4s ease;
  padding:25px;
  overflow-y:auto;
  z-index:2000;
  border-top-left-radius:20px;
  border-bottom-left-radius:20px;
}
.cart.open{right:0;}
.cart h2{text-align:center;margin-bottom:20px;color:#ee0979;}
.cart-item{
  display:flex;
  align-items:center;
  justify-content:space-between;
  margin:12px 0;
  border-bottom:1px solid #eee;
  padding-bottom:10px;
}
.cart-item img{
  width:55px;
  height:55px;
  border-radius:12px;
  object-fit:cover;
}
.remove-btn{background:none;color:red;border:none;cursor:pointer;font-weight:bold;}
.total{text-align:center;font-weight:bold;margin:15px 0;font-size:1.1rem;}
.checkout-btn{
  display:block;
  background:#ff6a00;
  color:white;
  text-align:center;
  padding:12px;
  border-radius:15px;
  margin-top:10px;
  text-decoration:none;
  transition:0.3s;
}
.checkout-btn:hover{
  background:#ee0979;
  transform:scale(1.05);
}

/* ===== FOOTER ===== */
footer{
  background:#222;
  color:white;
  padding:40px 30px;
}
footer .footer-container{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(200px,1fr));
  gap:20px;
}
footer h3{color:#ff6a00;margin-bottom:15px;}
footer a{display:block;color:white;margin:5px 0;transition:0.3s;}
footer a:hover{color:#ee0979;transform:translateX(5px);}
footer p{margin-top:15px;color:#aaa;font-size:0.9rem;}

/* ===== LOGIN MODAL ===== */
#loginModal{
  display:none;
  position:fixed;
  top:0;left:0;width:100%;height:100%;
  background:rgba(0,0,0,0.6);
  justify-content:center;align-items:center;
  z-index:3000;
}
#loginModal .modal-content{
  background:white;padding:30px;border-radius:25px;width:320px;text-align:center;
  position:relative;
}
#loginModal .modal-content h2{margin-bottom:15px;color:#ee0979;}
#loginModal .close-btn{
  position:absolute;top:10px;right:15px;font-size:22px;font-weight:bold;color:#ee0979;cursor:pointer;
}

/* ===== ANIMATIONS ===== */
@keyframes fadeIn{from{opacity:0;transform:scale(0.95);}to{opacity:1;transform:scale(1);}}
</style>
</head>
<body>

<!-- HEADER -->
<header>
  <h1>🛒 ShopEase</h1>
  <div class="search-login">
    <input type="text" placeholder="Search Products..." id="searchInput" oninput="searchProducts()">
    <button class="login-btn" onclick="openLogin()">Login</button>
    <div onclick="toggleCart()" style="cursor:pointer;">🛍️ Cart <span id="cart-count">0</span></div>
  </div>
</header>

<!-- SIDEBAR -->
<div class="sidebar" id="categorySidebar"></div>

<!-- PRODUCT GRID -->
<div class="container" id="productContainer"></div>

<!-- CART -->
<div class="cart" id="cart">
  <h2>Your Cart</h2>
  <div id="cart-items"></div>
  <div class="total" id="cart-total">Total: ₹0</div>
  <a href="#" class="checkout-btn">Checkout</a>
</div>

<!-- LOGIN MODAL -->
<div id="loginModal">
  <div class="modal-content">
    <span class="close-btn" onclick="closeLogin()">&times;</span>
    <h2>Login</h2>
    <input type="text" placeholder="Username" style="padding:7px 12px;border-radius:12px;width:100%;margin-bottom:10px;"><br>
    <input type="password" placeholder="Password" style="padding:7px 12px;border-radius:12px;width:100%;margin-bottom:15px;"><br>
    <button style="background:#ff6a00;color:white;padding:8px 18px;border:none;border-radius:25px;" onclick="alert('Login Successful!')">Login</button>
  </div>
</div>

<!-- FOOTER -->
<footer>
  <div class="footer-container">
    <div>
      <h3>About Us</h3>
      <p>ShopEase is a modern e-commerce platform for all your shopping needs.</p>
    </div>
    <div>
      <h3>Links</h3>
      <a href="#">Home</a>
      <a href="#">Products</a>
      <a href="#">Cart</a>
      <a href="#">Login</a>
    </div>
    <div>
      <h3>Contact</h3>
      <a href="#">Email: info@shopease.com</a>
      <a href="#">Phone: +91 1234567890</a>
      <a href="#">Address: India</a>
    </div>
    <div>
      <h3>Follow Us</h3>
      <a href="#">Facebook</a>
      <a href="#">Instagram</a>
      <a href="#">Twitter</a>
    </div>
  </div>
  <p>© 2025 ShopEase | Created by Shalini 💖</p>
</footer>

<script>
// CART
let cart=[];
function addToCart(name,price,img){
  cart.push({name,price,img});
  updateCart();
}
function removeFromCart(index){
  cart.splice(index,1);
  updateCart();
}
function updateCart(){
  const cartItems=document.getElementById("cart-items");
  const cartTotal=document.getElementById("cart-total");
  const cartCount=document.getElementById("cart-count");
  cartItems.innerHTML="";
  let total=0;
  cart.forEach((item,i)=>{
    total+=item.price;
    cartItems.innerHTML+=`
      <div class="cart-item">
        <img src="${item.img}" />
        <span>${item.name}</span>
        <span>₹${item.price}</span>
        <button class="remove-btn" onclick="removeFromCart(${i})">x</button>
      </div>`;
  });
  cartTotal.innerText="Total: ₹"+total;
  cartCount.innerText=cart.length;
}
function toggleCart(){document.getElementById("cart").classList.toggle("open");}

// LOGIN
function openLogin(){document.getElementById('loginModal').style.display='flex';}
function closeLogin(){document.getElementById('loginModal').style.display='none';}

// LOAD PRODUCTS FROM API (Multiple pages for more products)
let allProducts=[];
async function fetchProducts(){
  for(let skip=0; skip<200; skip+=100){
    let res=await fetch(https://dummyjson.com/products?limit=100&skip=${skip});
    let data=await res.json();
    allProducts.push(...data.products);
  }
  displayProducts(allProducts);
  loadCategories(allProducts);
}
fetchProducts();

function displayProducts(products){
  const container=document.getElementById('productContainer');
  container.innerHTML="";
  products.forEach(p=>{
    container.innerHTML+=`
      <div class="card ${p.category}">
        <img src="${p.thumbnail}" />
        <h3>${p.title}</h3>
        <p>₹${p.price}</p>
        <button onclick="addToCart('${p.title}',${p.price},'${p.thumbnail}')">Add to Cart</button>
      </div>`;
  });
}

// CATEGORIES
function loadCategories(products){
  const categories=[...new Set(products.map(p=>p.category))];
  const sidebar=document.getElementById('categorySidebar');
  sidebar.innerHTML=<button onclick="filterProducts('all')">All</button>;
  categories.forEach(cat=>{
    sidebar.innerHTML+=<button onclick="filterProducts('${cat}')">${cat.charAt(0).toUpperCase()+cat.slice(1)}</button>;
  });
}

// FILTER
function filterProducts(category){
  if(category==='all') displayProducts(allProducts);
  else displayProducts(allProducts.filter(p=>p.category===category));
}

// SEARCH
function searchProducts(){
  const query=document.getElementById('searchInput').value.toLowerCase();
  const filtered=allProducts.filter(p=>p.title.toLowerCase().includes(query));
  displayProducts(filtered);
}
</script>
</body>
</html>
